[project]
name = "complyaigent"
version = "0.1.0"
description = "AI‑powered DevSecOps compliance engine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
]

[tool.fastapi]
entrypoint = "main:app"
